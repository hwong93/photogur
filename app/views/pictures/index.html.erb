<h1>Pictures(<%= @pictures.length %>)</h1>
<%= link_to "New Picture", pictures_path %>

<% @pictures.each do |picture| %>
  <p><%= picture.title %>, <%= picture.artist %></p>
  <%= link_to image_tag(picture.url), picture_path(picture.id) %>
<% end %>
